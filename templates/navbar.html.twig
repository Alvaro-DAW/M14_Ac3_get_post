<!-- Navigation -->
<nav>
    <a href="{{ path('home') }}">Inici</a>
    
    <a href="{{ path('producte_list') }}">Producte</a>
    <a href="{{ path('categoria_list') }}">Categoria</a>
    
  
    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
    <a class="link" href="{{ path('app_logout') }}">Logout</a>
    {% else %}
     <a href="{{ path('app_login') }}">Login</a>
    {% endif %}
</nav>
<div class="content">
